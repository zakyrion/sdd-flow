# Subject

```clojure
{:task :fixture
 :stage :s1}
```

# s1

```clojure
(def fixture
  {:makes "the one result"
   :criterion "the rule"
   :data {:input {:type string :holds "what comes in" :from "the caller"}
          :spare {:type string :holds "what nobody reads" :from "the caller"}}
   :flow (-> (:step-1 "take what we work with"
                      {:reads #{} :writes #{:input} :state "the input stands"}))})
```

# Contra

```clojure
[]
```

# Gate

```clojure
{:gate-after-s1 {:subject "the algorithm"}}
```
