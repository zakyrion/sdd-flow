# s2

```clojure
(def Fixture-methods
  {run
   ^:added
   {:does "do the one deed"
    :in #{:input}
    :out :none
    :how "the mechanism"}})
```

```clojure
(def Fixture-data
  {:input {:type string :as input :lives :external :holds "what comes in"}})
```
