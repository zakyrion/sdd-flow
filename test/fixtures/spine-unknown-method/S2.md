# Subject

```clojure
{:task :fixture
 :subject "Fixture"
 :stage :s2
 :living-s2 :none}
```

# s2

```clojure
(def Fixture-methods
  {lint
   {:does "the entry point"
    :in #{:input}
    :out :none
    :flow (-> (:step-1 "fill the ledger" {:calls missing :out :none}))}})
```

```clojure
(def Fixture-data
  {:input {:type string :as input :lives :external :holds "what comes in"}})
```

# Contra

```clojure
[]
```

# Slices

```clojure
[{:slice :one
  :writes #{"src/fixture.js"}
  :carries #{lint}
  :after #{}
  :confidence 60}]
```

# Gate

```clojure
{:gate-after-s2 {:subject "the structure of the code"}}
```
