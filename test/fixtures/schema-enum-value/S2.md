# Subject

```clojure
{:task :fixture
 :subject "Fixture"
 :stage :s2
 :living-s2 :none}
```

# s2

```clojure
(def Fixture-methods
  {run
   {:does "do the one deed"
    :in #{:input}
    :out :none
    :how "the mechanism"}})
```

```clojure
(def Fixture-data
  {:input {:type string :as input :lives :nowhere :holds "what comes in"}})
```

# Contra

```clojure
[]
```

# Slices

```clojure
[{:slice :one
  :writes #{"src/fixture.js"}
  :carries #{run}
  :after #{}
  :confidence 60}]
```

# Gate

```clojure
{:gate-after-s2 {:subject "the structure of the code"}}
```
