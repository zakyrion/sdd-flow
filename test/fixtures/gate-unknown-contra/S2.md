# Subject

```clojure
{:task :fixture
 :subject "Fixture"
 :stage :s2
 :living-s2 :none}
```

# s2

```clojure
(def Fixture-methods
  {run
   {:does "do the one deed"
    :in #{:input}
    :out :none
    :how "the mechanism"}})
```

```clojure
(def Fixture-data
  {:input {:type string :as input :lives :external :holds "what comes in"}})
```

# Contra

```clojure
[{:id :c-1
  :kills "what it breaks"
  :case "a concrete line"
  :fails "what exactly is wrong"
  :fix [{:id :fix-a :confidence 60 :is "the option" :cost "what it costs"}]}]
```

# Slices

```clojure
[{:slice :one
  :writes #{"src/fixture.js"}
  :carries #{run}
  :after #{}
  :confidence 60}]
```

# Gate

```clojure
{:auto-decided [{:id :ad-1 :auto-decided true :confidence 70
                 :chosen "the one"
                 :options [{:option "the one" :confidence 70} {:option "the other" :confidence 30}]
                 :because "why the rating"
                 :answers :c-9}]
 :gate-after-s2 {:subject "the structure of the code"}}
```
